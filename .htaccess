RewriteEngine On

# Force HTTPS
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Redirect www -> non-www
RewriteCond %{HTTP_HOST} ^www\.caleanafestival\.fr$ [NC]
RewriteRule ^ https://caleanafestival.fr%{REQUEST_URI} [R=301,L]
